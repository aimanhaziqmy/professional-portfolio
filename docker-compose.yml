services:
  portfolio:
    build: .
    ports:
      - "8080:80"
    container_name: aiman-portfolio
    restart: always
# for traefik implementation
#     labels:
#       - traefik.enable=true
#       - traefik.http.routers.portfolio.rule=Host(`portfolio.aimanhaziq.my`)
#       - traefik.http.routers.portfolio.entrypoints=websecure
#       - traefik.http.routers.portfolio.tls=true
#       - traefik.http.routers.portfolio.tls.certresolver=apacheresolver
#       - traefik.http.services.portfolio.loadbalancer.server.port=80
#     networks:
#       - proxy

# networks:
#   proxy:
#     external: true